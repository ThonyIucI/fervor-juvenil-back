# ========================================
# CONFIGURACIÓN DE ENTORNO
# ========================================
#
# Copia este archivo a .env y configura los valores según necesites.
# Puedes cambiar entre entornos simplemente modificando las variables.
#
# IMPORTANTE:
# - Nunca commitees el archivo .env (ya está en .gitignore)
# - Mantén este .env.example actualizado como referencia
#
# ========================================

# ========================================
# OPCIÓN 1: Desarrollo local con Docker
# ========================================
# Usa estas variables cuando trabajes con Docker Compose local

DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=rootpassword
DB_NAME=fervor_juvenil

# Deja DATABASE_URL comentado para desarrollo local
# DATABASE_URL=

# ========================================
# OPCIÓN 2: Conectar a base de datos remota (Render, etc)
# ========================================
# Descomenta DATABASE_URL y comenta las variables DB_* individuales
# La app detectará DATABASE_URL automáticamente y lo usará con prioridad

# DATABASE_URL=postgresql://fervor_juvenil_user:contraseña@dpg-xxxxx.oregon-postgres.render.com/fervor_juvenil

# ========================================
# CONFIGURACIÓN PARA DEPLOY EN RENDER
# ========================================
# En Render, configura estas variables de entorno en el dashboard:
#
# 1. DATABASE_URL → Render la inyecta automáticamente al conectar PostgreSQL
# 2. JWT_SECRET → Genera uno seguro (ej: openssl rand -base64 32)
# 3. PORT → Render usa su propio $PORT, NO configures esto manualmente
# 4. NODE_ENV → Configura como "production"
#
# Opcionales (si usas Google OAuth):
# 5. GOOGLE_CLIENT_ID
# 6. GOOGLE_CLIENT_SECRET
# 7. GOOGLE_CALLBACK_URL → https://tu-app.onrender.com/api/auth/google-redirect

# ========================================
# CONFIGURACIÓN GENERAL
# ========================================

# Puerto de la aplicación
PORT=3006

# JWT Secret (genera uno seguro para producción)
JWT_SECRET=dev_jwt_secret_change_in_production

# Google OAuth (opcional)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:3006/api/auth/google-redirect
